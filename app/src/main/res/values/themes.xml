<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.LiveStream" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/black</item>

        <!--   color for the app bar and other primary UI elements -->
        <item name="colorPrimary">@android:color/white</item>

        <!--   a darker variant of the primary color, used for
          the status bar (on Android 5.0+) and contextual app bars -->
        <item name="colorPrimaryDark">@color/black</item>

        <!--   a secondary color for controls like checkboxes and text fields -->
        <item name="colorAccent">@android:color/white</item>
        <item name="android:fontFamily">@font/manrope</item>
        <item name="fontFamily">@font/manrope</item>
        <item name="android:windowBackground">@color/black</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
    </style>

    <style name="liveUserProfileImageView" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">@dimen/size_12</item>
    </style>

    <style name="TextInputLayout" parent="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox.Dense">
        <item name="boxStrokeWidth">@dimen/size_0</item>
        <item name="hintEnabled">false</item>
        <item name="boxStrokeWidthFocused">@dimen/size_0</item>
        <item name="boxBackgroundColor">@color/charleston_green</item>
        <item name="android:textColorHint">@color/white</item>
        <item name="hintTextColor">@color/white</item>
        <item name="android:textColor">@color/white</item>
        <item name="endIconMode">clear_text</item>
        <item name="boxCornerRadiusBottomEnd">@dimen/size_0</item>
        <item name="boxCornerRadiusBottomStart">@dimen/size_0</item>
        <item name="boxCornerRadiusTopEnd">@dimen/size_0</item>
        <item name="boxCornerRadiusTopStart">@dimen/size_0</item>
    </style>
    <!-- Base application theme. -->
    <style name="Base.Theme.Livestream" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Livestream" parent="Base.Theme.Livestream" />
</resources>